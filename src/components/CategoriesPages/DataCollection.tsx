import { Accordion, AccordionButton, AccordionItem, AccordionPanel, VStack } from "@chakra-ui/react";

export function DataCollection() {

  return (
    <VStack>
      <Accordion>
        <AccordionItem>
          <AccordionButton></AccordionButton>

          <AccordionPanel>

          </AccordionPanel>
        </AccordionItem>
      </Accordion>
    </VStack>
  )
}